---
name: configure
description: Configure {{ plugin.name }} settings — vault name, vault path, qmd collection, and folder structure. Use when the user says "configure obsidian", "set vault path", "obsidian config", "change vault", "switch vault", or when another {{ plugin.name }} skill reports that config is missing.
version: 0.2.0
user-invocable: true
allowed-tools: Bash(qmd *), Bash(obsidian *), Bash(pgrep *), Bash(mkdir *), Bash(nohup *), Read, Write, Glob, AskUserQuestion
---

{% include "shared/skills/configure.body.md.j2" %}
